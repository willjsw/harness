# {{ADR_DIR}}

MADR 방식 결정 기록. **운영 규칙의 정본은 `.ai/adr.md` 다** — 명령·상태·불변식·판단 기준이
거기 있고, 하네스 설정에서 생성된다.

이 디렉터리에는 결정 기록 문서와 그 템플릿만 둔다.

- 파일명은 `NNNN-title-with-dashes.md`
- `templates/template.md` 가 새 문서의 골격이다
- 상태는 본문이 아니라 front matter 의 `status:` 한 줄이 갖는다
- 본문은 프로젝트 문서 언어로, 제목·파일명·절 이름은 영어로

**기존 문서의 본문을 고치지 않는다.** 기록은 로그이고, 틀린 결정도 그대로 남아야 한다.
번복은 새 문서로 하고 기존 문서는 front matter 의 상태 한 줄만 바뀐다.
