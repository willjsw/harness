<!--
이 파일은 프로젝트가 소유한다. 하네스 갱신이 덮지 않는다.
**보호 문서다** — 에이전트가 근거로 읽고 스스로 고치지 못한다.

실제 디렉터리 구조에서 읽어낸 것만 적는다. 없는 구조를 지어내지 않는다.
-->

### 구성 요소

<!-- TBD: 시스템을 이루는 덩어리(화면·서버·DB·외부 서비스)와 각각의 기술. 한 줄에 하나 -->

### 데이터 흐름

<!-- TBD: 요청이 어디서 들어와 어디로 가는지 한 줄. 예: 사용자 → 화면 → API → DB -->

### 신뢰 경계

<!-- TBD: 밖에서 들어오는 입력(요청·파일·웹훅·메시지)과 다루는 민감 정보의 종류.
     값은 적지 않는다. 보안 점검이 검증 없이 들어오는 값과 새면 안 되는 값을 찾는 기준이다. -->

### 새 코드를 둘 곳

<!--
새 구조를 만들기 전에 기존 방식으로 해결되는지 먼저 확인하게 하는 항목들. "무엇 → 어디" 한 줄에 하나.
**최소 둘은 채운다** — 비면 리뷰어가 과설계를 지적할 근거를 잃는다.
-->

- <!-- TBD -->
- <!-- TBD -->

### 계층과 의존 방향

<!-- TBD: 디렉터리별 역할과 누가 누구를 import 할 수 있는지 -->

### 검사하는 것

기계로 검사하는 규칙은 이 문서가 아니라 `harness.toml` 의 `[verify]` 에 둔다 — 커밋 뒤와 CI 에서 실제로 돈다.
목록은 `.ai/AI_AGENT.md` 3장에 있다.

### 검사하지 않는 것 (현재 상태 기록)

<!-- TBD: 바람직하지 않지만 이미 존재하는 패턴. 규칙으로 올리려면 먼저 걷어내야 한다.
     없으면 "없음" 이라고 적는다. -->
