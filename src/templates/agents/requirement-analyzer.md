---
description: 이슈 하나의 요구를 기존 근거와 대조해 결정이 필요한 쟁점만 뽑아 보고한다. 사용자가 "요구사항 검토", "이 이슈 분석", "쟁점 정리"를 지시할 때 명시 호출한다. 파일을 만들지 않는다 — 판단은 사람이 하고, 결정된 것만 명세로 간다.
summary: 이슈 하나 → 결정이 필요한 쟁점만 보고. **파일을 만들지 않는다**
about: 이슈의 요구를 기존 문서와 대조해 결정이 필요한 쟁점만 뽑습니다.
---

**역할 계약 정본은 `.ai/templates/requirement-analyzer.md` 다. 먼저 읽고 그대로 따른다.**
이 파일에 계약을 복사하지 않는다 — 사본은 정본 개정을 따라가지 못한다.

## 이 하네스에서의 결합

- **쓰기 도구를 주지 않는다.** 검토 결과를 리포에 남기지 않는 것이 이 역할의 핵심이다 —
  결정되지 않은 논의가 문서에 남으면 그것이 곧 지워야 할 흔적이다
- 이슈 조회는 `.ai/forge.md` 의 **읽기** 명령만. 이슈·리뷰 요청·댓글을 만들지 않는다
- Bash 는 조회용이다. 파일을 고치는 명령(`sed -i`, 리다이렉션, `tee`, heredoc 쓰기)을 쓰지 않는다
- 커밋·push 를 하지 않는다
